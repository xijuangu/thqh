<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.processserver.mapper.db1.SyncStateTableMapper">
    <!--    List<SyncStateTable> searchAll();-->
    <select id="searchAll" resultType="org.example.processserver.pojo.SyncStateTable">
        SELECT * FROM SyncStateTable
    </select>

    <!--    int update(@Param("syncstate")int syncstate,@Param("checkTime")String checkTime,@Param("sourceCount")Integer sourceCount,@Param("targetCount")Integer targetCount,@Param("checkType")String checkType,@Param("tableName")String tableName);-->
    <update id="update">
        UPDATE SyncStateTable SET syncstate = #{syncstate},checkTime = #{checkTime}, sourceCount = #{sourceCount},
        targetCount = #{targetCount}, checkType = #{checkType} WHERE tableName = #{tableName}
    </update>


</mapper>
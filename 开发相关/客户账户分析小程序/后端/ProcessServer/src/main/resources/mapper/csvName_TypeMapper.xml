<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.processserver.mapper.db1.csvName_TypeMapper">
<!--    csvName_Type getCsvName_Type_one(@Param("csvName") String csvName);   -->
    <select id="getCsvName_Type_one" resultType="org.example.processserver.pojo.csvName_Type">
        SELECT * FROM CSVFileName where csvName = #{csvName}
    </select>

<!--    List<csvName_Type> getCsvName_Type_all();-->
    <select id="getCsvName_Type_all" resultType="org.example.processserver.pojo.csvName_Type">
        SELECT * FROM CSVFileName
    </select>
</mapper>